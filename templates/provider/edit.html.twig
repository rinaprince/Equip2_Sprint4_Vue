{% extends 'base_back_3.html.twig' %}

{% block title %}Edit Provider{% endblock %}

{% block body %}
    <h1>Edit Provider</h1>
    <div {{ vue_component('Hello', { 'name': 'Estas editant un proveidor' }) }}></div>

    {{ include('provider/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_provider_index') }}">back to list</a>

    {{ include('provider/_delete_form.html.twig') }}
{% endblock %}
